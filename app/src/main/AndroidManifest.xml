<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.gpginc.ntateam.apptest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GameEnd"
            android:label="@string/title_activity_game_end"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".LoadingGame"></activity>
        <!--
activity
            android:name=".Settings"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" /
        -->
        <activity
            android:name=".MainPlusSettings"
            android:label="@string/title_activity_main_plus_settings"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DamageStep"
            android:label="@string/title_activity_damage_step"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PrePlayer"
            android:label="@string/title_activity_pre_player"
            android:theme="@style/AppTheme" />
        <activity android:name=".SkillRun" />
        <activity android:name=".CurrentPlayer" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>